import { IconClassPipe } from './icon-class.pipe';

describe('IconClassPipe', () => {
  it('create an instance', () => {
    const pipe = new IconClassPipe();
    expect(pipe).toBeTruthy();
  });
});
